let arr = [1, 2, 3, 4];
function check(ele) {
  return ele > 0;
}
arr.find(check);
//  find lo call back pass chestam  call back lo condition rastamu a condition satisfy ayyaka 1st number vastadi
